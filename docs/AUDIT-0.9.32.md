# Audit contract — 0.9.32

All 85 previous mandatory commands remain in order. Four additional suites bring
the inventory to 89 commands. A nonzero status remains a release-blocking failure.

Added coverage: conservative Docker inventory/ownership planning, real file locking,
race checks, protected production and rollback, durable JSON records, receipt-failure
rollback, encoded sidecar paths, error encoding and byte-range representation.

The downloadable validation report distinguishes real PHP/Apache/Git/filesystem
execution from Docker/SSH/API simulations. A simulated pass is not a native VPS pass.
The serving Alpine/event/FPM audit and live provider gates still run on the candidate.
HTTP 429, missing evidence, parser failures and failed rollback are never successes.

No local PHP or Guix environment download is required by the two Fish operators.
