# v0.9.26 audit scope

Audited from exact published v0.9.25 source tree
fdf685c9ba88ce6228d118f9a1eca476895b763b. The complete kit audit and raw logs describe
actual executed checks. Original runtime defects were reproduced using the prior
Google adapter and explicitly isolated network doubles: zero-height DivisionByZeroError,
malformed optional-field TypeError, inert-prefix format rejection, and an explicit
next-page offset discarded by the exact-result-count flag. No live visitor query or
response was captured. Fixed flows execute the real adapter methods with labelled
transport doubles; these are not native-extension/live-provider success claims.

All previous 61 command entries remain in order; six additional mandatory commands
bring the total to 67. Missing native PHP extensions or fish are nonzero results,
not passes. No live benchmark comparison is run or published. Browser captures are
local actual-PHP document fixtures with embedded resources because native browser
navigation is blocked by managed policy. This does not establish Lighthouse metrics,
production cache behavior or live Google availability.

The explicit --verify-google candidate gate tests web and images before production
cutover and writes only safe metadata. Existing readiness, RSS and Binternet checks
remain. No test, SSL validation, response limit or host policy is disabled to pass.
Source packages exclude the private operator artwork on all forges. Earlier release
tags, repository history and backup directories are not rewritten.

## Executed checks

* 60 pure PHP CSE protocol/record/pagination assertions passed.
* 15 actual adapter web/image-flow assertions passed with explicit child-process
  cURL doubles; 9 session-lock/race assertions passed with isolated APCu doubles.
* 6 single-pass template/actual-PHP HTTP and compiled-skin checks passed.
* 8 candidate Google-report tests passed with Docker subprocesses mocked.
* 27 version-specific publication/package tests passed with temporary Git repositories
  and mocked APIs. Local workflows and complete final-tree evidence are in the kit.
* 24 Chromium layout/color comparisons passed across six themes and two widths,
  with appearance controls both open/closed. Both READMEs use the local capture.
  The existing browser-local picture fixture also passed 33 real decoding/control
  checks with the same embedded-document navigation limitation.
* The final source-archive audits, with and without a synthetic private-theme pack,
  attempt all 67 commands. Each completes 48 successfully; 19 are nonzero because
  required native PHP extensions or fish are unavailable. The runner returns 1.
  This is not a complete native Docker pass and is not relabelled as success.

The original benchmark script is byte-identical to the upload. Its embedded Python
syntax was checked without running it. Native fish parsing is unavailable here.
There are no generated comparative benchmark outputs in the source or kit.

Early full-source runs caught stale asset29 and 61-command assertions after the
release bump, plus the retained historical-tag wording expected by attribution tests.
The expectations/documentation were updated, not skipped. Native search HTTP tests
remain blocked by mbstring and the other required extensions; missing extensions
were not substituted or masked. Local parity byte measurements are structural
checks, not internet latency estimates or a new leaderboard.
